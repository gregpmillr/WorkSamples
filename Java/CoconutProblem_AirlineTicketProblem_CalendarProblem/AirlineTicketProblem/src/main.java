/**
 * Represents a driver for the TestTicket class which is used to test the
 * functionality of this program.
 * <p>
 * When run this program will:
 * <ul>
 * <li>Test the creation of AirlineTicket, Customer, and Flight
 * <li>Test the methods of each class to ensure proper functionality
 * <li>Tests the integrity of object relationships (e.g. subclass AirlineTicket to superclass Flight)
 * <li>Prints out each class.
 * </ul>
 * <p>
 * Assumptions/Restrictions: None - does only what was asked for in the assignment
 * <p>
 * Noteworthy Features: None - does only what was asked for in the assignment
 * @author Greg
 */
public class main {

	/**
	 * Driver which instantiates the TestTicket class and starts the testing
	 * processes.
	 * 
	 * @param args main input for program
	 */
	public static void main(String[] args) {
		TestTicket.start();
	}

}
